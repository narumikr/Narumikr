# スクリプト使い方

`/scripts`フォルダ内のファイルの使い方など記載

## _generate_gallery_

### 用途

README.mdファイルの**gallery**セクションを自動生成するスクリプト

**/assets/gallery**フォルダに配置した画像を更新日時が新しいもの順にグリッド配置する

### 使い方

ルートディレクトリで以下を実行

```bash
py scripts/generate_gallery.py
```